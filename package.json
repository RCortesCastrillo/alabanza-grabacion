{
  "name": "alabanza-grabacion",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rm -rf dist/.git && cd dist && touch .nojekyll && git init -q -b gh-pages && git add -A && git commit -q -m deploy && git push -f https://github.com/RCortesCastrillo/alabanza-grabacion.git gh-pages; cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "@breezystack/lamejs": "^1.2.7",
    "idb-keyval": "^6.2.1"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.0"
  }
}
